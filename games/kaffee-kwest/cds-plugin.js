// Intentionally empty — marks this package as a CAP plugin so its
// package.json "cds" section is merged into cds.env:
//   games.kaffee-kwest        → game + UI registration (platform registry)
//   requires.kaffee-kwest     → own CDS model (persistence + KaffeeKwestService)
